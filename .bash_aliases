# git commands
alias g="git"
alias gcm="git commit -m"
alias gpso="git push origin"
alias gplo="git pull origin"

# kubectl commands
alias kc="kubectl"

# grep commands: enable coloring
alias grep="grep --color=auto"

# list commands aliases
alias l="ls -l"
alias la="ls -lA"
